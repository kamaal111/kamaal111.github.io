import React from 'react';

import { copyWrite } from '../config';

function Footer() {
  return (
    <footer>
      <div>
        <p>{copyWrite}</p>
      </div>
    </footer>
  );
}

export default Footer;
