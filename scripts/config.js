const domainName = 'kamaal.io';
const teamIdentifier = 'DXUKH9VF73';
const apps = {
  ColorPicker: {
    name: 'ColorPicker',
    appId: `${teamIdentifier}.${domainName}.ColorPicker`,
  },
};

module.exports = {
  domainName,
  teamIdentifier,
  apps,
};
